---
paths:
  - 'src/**/*.{ts,tsx}'
---

# React and TypeScript conventions

AGENTS.md carries the conventions themselves. The worked examples — composable slots, the three `useEffect` anti-patterns, and the `useMemo` + cleanup recipe for side-effectful values — are in [docs/REACT-TYPESCRIPT.md](../../docs/REACT-TYPESCRIPT.md). Read it before writing a new component or reaching for an effect.
